using UnityEngine;

public class NeverStopMusic : MonoBehaviour
{
    private static NeverStopMusic instance;

    void Awake()
    {
        if(instance == null)
        {
            instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
